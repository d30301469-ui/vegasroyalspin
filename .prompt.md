# .prompt.md — Draft

Purpose
- Provide a reusable prompt for the common task: creating or customizing prompts for `agent-customization` workflows.

Core task
- Given a short description of a developer task pattern (what you repeat), generate a concise, repeatable prompt that: (1) states the task clearly, (2) lists required inputs and optional context, (3) specifies desired output format, and (4) includes 2 example invocations.

Inputs (template)
- `task_description` (required): One-sentence description of the recurring developer task.
- `scope` (optional): `workspace` or `personal` — whether the prompt uses repository files or personal preferences.
- `files` (optional): List of workspace paths to include by default.
- `constraints` (optional): Any style, length, or output-format constraints.

Outputs (template)
- A single prompt string optimized for use with Copilot/agents that:
  - Accepts the `task_description` and optional fields.
  - Produces step-by-step actionable instructions and example invocations.

Invocation examples
- Example 1 (refactor pattern):
  - Input: `task_description=Refactor controller methods to service classes; scope=workspace; files=controllers/*.php`
  - Output: Prompt that instructs an agent to analyze specified controller files and produce service-class stubs and a migration plan.
- Example 2 (tests):
  - Input: `task_description=Add unit tests for repository functions; scope=workspace` 
  - Output: Prompt that instructs the agent to generate PHPUnit tests and example assertions.

Ambiguities to decide
- Should the prompt run with live workspace files by default or only when explicitly given `files`?
- Preferred output verbosity: terse vs. detailed step-by-step?
- Where to store finalized prompts: repo root, `.copilot/`, or personal prompts folder?

Next steps for you
1. Choose `scope` (`workspace` or `personal`).
2. Provide one or two concrete `task_description` examples you repeat.
3. Indicate preferred verbosity (`terse` or `detailed`).

Suggested related customizations
- Create a `.prompt.md` variant for `code-review` tasks.
- Add a `prompts/` folder with named prompt files for common workflows.

---
Generated by assistant — draft. Please tell me which of the "Ambiguities to decide" options you prefer and provide one example `task_description` to finalize this prompt.
